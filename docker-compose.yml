version: '3.8'

services:
  app:
    build: .
    restart: always
    container_name: altar-api-service
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - altar-network

networks:
  altar-network:
    external: true